language: generic 

sudo: required
services:
  - docker

before_install:
  - docker build -t nnhuyhoang/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true nnhuyhoang/docker-react npm run test -- --coverage